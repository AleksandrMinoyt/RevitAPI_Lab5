<Window x:Class="RevitAPI_WPFCreateButton.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RevitAPI_WPFCreateButton"
             mc:Ignorable="d"             
             Title="Некоторые свойства модели" Width="300" Height="170">
    <StackPanel>
        <Label Content="Нажмите на одну из кнопок:" Margin="5" />
        <Button Content="Количество труб" Command="{Binding CountPipes}" Margin="5"/>
        <Button Content="Объём всех стен" Command="{Binding VolumeWalls}" Margin="5"/>
        <Button Content="Количество дверей" Command="{Binding CountDoors}" Margin="5"/>
    </StackPanel>
</Window>
